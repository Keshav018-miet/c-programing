#include<stdio.h>
int main() {
    int x, y;
    printf("enter the value of x and y\n");
    scanf("%d%d", &x, &y);
    if (x > y) (printf("x is greater than y\n") ) ; ( printf("y is greater than x\n") );   
    return 0;
}
